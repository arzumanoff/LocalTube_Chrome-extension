# Development tool bundle

For the current Windows development build, place these executables here before running `install_host.ps1`:

- `ffmpeg.exe`
- `ffprobe.exe`
- `deno.exe`

The future public installer will bundle and install verified versions automatically. These binaries are intentionally not committed to Git.
