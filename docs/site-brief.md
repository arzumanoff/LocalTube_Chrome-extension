# Бриф для сайта Media Downloader

> Документ для Kimi — содержит все факты, необходимые для создания лендинга и страницы загрузки.

---

## 1. Продукт

**Media Downloader** — бесплатное Chrome-расширение с открытым исходным кодом для скачивания видео с YouTube в MP4 (H.264/AAC).

Не требует регистрации, не отправляет данные на сторонние серверы, не содержит рекламы.

---

## 2. Целевая аудитория

Обычные пользователи, которые хотят сохранить YouTube-видео на компьютер в хорошем качестве без возни с командной строкой.

---

## 3. Ключевые возможности (текущий MVP)

- Кнопка **«Скачать»** под плеером YouTube
- **Реальные качества** — расширение показывает только те разрешения, которые действительно доступны для конкретного видео (360p, 720p, 1080p, 1080p60, 1440p, 4K…)
- Никакого навязывания фиксированного списка — если видео есть в 1080p60, вы увидите «1080p60»; если только в 30 fps — просто «1080p»
- Системный диалог **«Сохранить как»** — вы выбираете имя файла и папку
- Автоматическая конвертация в H.264/AAC MP4 (совместимость с любым плеером)
- Прогресс скачивания и возможность отмены
- Никакие URL, куки, заголовки видео не передаются наружу

---

## 4. Как это работает (простое объяснение)

Расширение не качает видео само. Оно передаёт ссылку на YouTube локальному движку на вашем компьютере, который через проверенные инструменты (`yt-dlp` + FFmpeg) скачивает видео и аудио раздельно, склеивает в MP4 и при необходимости перекодирует в H.264/AAC.

```
YouTube → Кнопка «Скачать» → Локальный движок → MP4-файл
```

Ни один байт видео не проходит через сторонний сервер.

---

## 5. Технический стек

| Слой | Технология |
|------|-----------|
| Расширение | JavaScript, Manifest V3, Chrome Extension API |
| Протокол связи | Chrome Native Messaging |
| Движок | Python 3.12 (собран PyInstaller в .exe) |
| Скачивание | yt-dlp |
| Обработка медиа | FFmpeg + FFprobe |
| Сборка | PowerShell-скрипты |
| CI/CD | GitHub Actions (Windows) |

---

## 6. Текущий статус

**Версия:** 0.2.8 — активная разработка, dev-установка.

**Что уже работает:**
- Скачивание одиночных видео с реальными качествами
- Save As диалог
- Прогресс и отмена
- Авто-определение аппаратных энкодеров (NVIDIA NVENC, AMD AMF, Intel QSV)

**Что в разработке:**
- Плейлисты
- YouTube Shorts
- Сайт

**Автосборка инсталлятора:** при каждом теге `v*` GitHub Actions собирает `MediaDownloader-Setup.exe` и выкладывает в [GitHub Releases](https://github.com/arzumanoff/youtube-downloader-chrome-extension/releases).

---

## 7. Структура сайта

Сайт должен быть **одностраничным лендингом** со следующими секциями:

### 7.1 Hero-секция
- Название: **Media Downloader**
- Подзаголовок: «Скачивайте YouTube-видео в настоящем качестве. Без рекламы, без регистрации, без сторонних серверов.»
- Кнопка: **Скачать** (ссылка на последний GitHub Release: `https://github.com/arzumanoff/youtube-downloader-chrome-extension/releases/latest`) + **GitHub** (ссылка на репозиторий)

### 7.2 Как это работает
Три шага с иконками:
1. Откройте любое видео на YouTube → появляется кнопка «Скачать»
2. Выберите качество из реально доступных
3. Сохраните MP4-файл через обычный диалог Windows

### 7.3 Возможности
Иконки/карточки с ключевыми функциями:
- Настоящие качества (не фиксированный список)
- H.264/AAC MP4 — совместимость с любым плеером
- Приватность — всё остаётся на вашем компьютере
- Прогресс и отмена в реальном времени
- Открытый исходный код

### 7.4 Установка
Пошаговая инструкция (см. раздел 8 ниже).

### 7.5 Дорожная карта
Что уже работает и что будет дальше:
- ✅ Одиночные видео с реальными качествами
- ✅ Save As, прогресс, отмена
- 🔜 Плейлисты
- 🔜 YouTube Shorts
- 🔜 Публичный инсталлятор в один клик
- 🔜 Поддержка других браузеров

### 7.6 Футер
- Ссылка на GitHub: `https://github.com/arzumanoff/youtube-downloader-chrome-extension`
- Лицензия (определить)
- «Используйте только для материалов, которые вы вправе скачивать»

---

## 8. Инструкция по установке (для сайта)

### Способ 1: Инсталлятор (рекомендуемый)

1. Скачайте `MediaDownloader-Setup-*.exe` с [GitHub Releases](https://github.com/arzumanoff/youtube-downloader-chrome-extension/releases/latest)
2. Запустите установщик — он:
   - Установит локальный движок (yt-dlp + FFmpeg + Deno)
   - Автоматически определит ваш видеоэнкодер (NVIDIA/AMD/Intel)
   - Зарегистрирует Native Messaging host
   - Скопирует расширение в `%LOCALAPPDATA%\MediaDownloader\extension\`
3. После установки откроется `chrome://extensions`
4. Включите «Режим разработчика» → «Загрузить распакованное расширение» → выберите папку расширения
5. Откройте YouTube, нажмите Ctrl+F5

### Способ 2: Ручная установка (для разработчиков)

Требуется: Python 3.12+, Git, Node.js 20+

```bash
git clone https://github.com/arzumanoff/youtube-downloader-chrome-extension.git
cd youtube-downloader-chrome-extension
```

Положите `ffmpeg.exe`, `ffprobe.exe` и `deno.exe` в `native-host/tools/`, затем:

```powershell
Set-ExecutionPolicy -Scope Process Bypass
.\native-host\build_host.ps1
.\native-host\install_host.ps1
```

Загрузите корневую папку как распакованное расширение в Chrome.

### Удаление

- **Инсталлятор:** через «Установка и удаление программ» Windows
- **Ручная:** `.\native-host\uninstall_host.ps1`

---

## 9. Ссылки

- **GitHub:** `https://github.com/arzumanoff/youtube-downloader-chrome-extension`
- **Issues:** `https://github.com/arzumanoff/youtube-downloader-chrome-extension/issues`

---

## 10. Дизайн-рекомендации

- **Тон:** дружелюбный, технически грамотный, без маркетинговых преувеличений
- **Цветовая гамма:** нейтральная, чистая — белый фон, тёмный текст, один акцентный цвет (например, синий или красный YouTube)
- **Язык:** русский (основной), возможно английская версия позже
- **Акцент на приватность** — значок замка, фраза «всё остаётся на вашем компьютере»
- **Никаких фейковых цифр** («10 000 скачиваний!») — проект новый, честность важнее

---

## 11. Файлы для размещения на сайте

Сайт не хранит файлы сам — все ссылки ведут на GitHub Releases.

1. **Кнопка «Скачать»** → `https://github.com/arzumanoff/youtube-downloader-chrome-extension/releases/latest`
   - Автоматически отдаёт последний `MediaDownloader-Setup-*.exe`
2. **Кнопка «GitHub»** → `https://github.com/arzumanoff/youtube-downloader-chrome-extension`
3. **Версия и changelog** — можно тянуть через GitHub API:
   - `GET https://api.github.com/repos/arzumanoff/youtube-downloader-chrome-extension/releases/latest`

### Как сайт узнаёт последнюю версию

```javascript
// Пример: получить ссылку на последний релиз
fetch('https://api.github.com/repos/arzumanoff/youtube-downloader-chrome-extension/releases/latest')
  .then(r => r.json())
  .then(release => {
    const version = release.tag_name;
    const exeAsset = release.assets.find(a => a.name.endsWith('.exe'));
    const downloadUrl = exeAsset.browser_download_url;
    // Показать кнопку «Скачать v{version}»
  });
```

Никакого хостинга файлов на сайте не требуется — GitHub делает всё.

---

## 12. Ключевые сообщения (для текстов на сайте)

| Тема | Сообщение |
|------|----------|
| Суть | «Скачивайте YouTube-видео в том качестве, в котором они есть на самом деле» |
| Приватность | «Ни один байт вашего видео не покидает компьютер» |
| Честность | «Показываем только те разрешения, которые реально доступны — без накруток» |
| Простота | «Кнопка Скачать → Качество → Готово» |
| Открытость | «Открытый исходный код на GitHub — смотрите, предлагайте, улучшайте» |