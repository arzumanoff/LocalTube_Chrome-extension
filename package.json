{
  "name": "youtube-downloader-chrome-extension",
  "version": "0.1.5",
  "private": true,
  "description": "Manifest V3 Chrome extension for downloading permitted YouTube media.",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "check": "node scripts/verify-extension.mjs",
    "e2e": "node scripts/chrome-e2e.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "puppeteer-core": "24.34.0"
  }
}
