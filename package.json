{
  "name": "youtube-downloader-chrome-extension",
  "version": "0.1.6",
  "private": true,
  "description": "Manifest V3 Chrome extension using a local yt-dlp/FFmpeg Native Messaging engine.",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "test:host": "python -m unittest discover -s native-host/tests -v",
    "check": "node scripts/verify-extension.mjs",
    "smoke:manual": "node scripts/manual-smoke.mjs",
    "e2e": "node scripts/manual-smoke.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "puppeteer-core": "24.34.0"
  }
}
